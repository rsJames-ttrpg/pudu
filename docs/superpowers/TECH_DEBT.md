# Pudu Tech Debt Ledger

Items discovered during stage reviews that were deliberately deferred. Each
stage's brainstorm should skim this for items targeted at the upcoming stage;
the planner folds them in as explicit task items. Rows marked ~~closed~~ are
kept for the record rather than deleted.

Every open row was re-checked against the code on 2026-08-30 (the branch's
final fix wave). Closing a row means striking it here in the same commit as
the code: two rows had shipped fixed-but-open, which is how the ledger stops
being trustworthy.

| ID | Opened | Target | Description |
|---|---|---|---|
| TD-S0-01 | 2026-08-30 | S9 | `ConfigError::NoPlatforms`'s message ("no platforms configured") names neither a field nor a file, a literal miss against the error-message contract stated in `src/error.rs`'s own doc comment. |
| TD-S0-02 | 2026-08-30 | ~~closed~~ | `Libc::as_npm()` and `Libc::short()` have no direct unit test. A swapped `gnu`/`musl` mapping is currently caught only indirectly, via Task 7's platform-name assertions. — ✅ closed in tech-debt batch B (`libc_as_npm_spells_glibc_and_musl` and `libc_short_form_feeds_generated_platform_names` in `src/platform.rs`; the row was left open by mistake and struck during the 2026-08-30 ledger audit) |
| TD-S0-03 | 2026-08-30 | ~~closed~~ | `[fixups] registry = "file://"` parses to `File("")` and `file://relative` to `File("relative")`; validation should reject an empty or relative registry path. — ✅ closed in the S0 final fix wave (`BadFixupRegistryPath`; empty and relative `file://` paths rejected) |
| TD-S0-04 | 2026-08-30 | S9 | Because `RawRegistry` uses `#[serde(flatten)]`, a typo'd `[registry]` key (`defualt = …`) is silently collected as a scope. `BadRegistryScope` (every scope must start with `@`) is the only backstop. |
| TD-S0-05 | 2026-08-30 | S4 | Neither `Platform` nor `Config` derives `Serialize`, and `FixupRegistry` has no `Display` back to its `github.com/owner/repo` form. `pudu init` hand-renders `pudu.toml`; a round-trip serializer would be sturdier. |
| TD-S0-06 | 2026-08-30 | ~~closed~~ | Stub stage labels for `fixups` (S7/S8) and `unused` (Phase 2) are asserted by no test; only `vendor`, `buckify`, and `audit` are covered. — ✅ closed in the S0 final fix wave (insta help snapshot pins all five `[UNIMPLEMENTED — …]` markers incl. `S7/S8` and `Phase 2`) |
| TD-S0-07 | 2026-08-30 | ~~closed~~ | `#[allow(dead_code)]` on the three `tests/common/mod.rs` helpers is inert today — all three are used. Drop it if it stays unnecessary. — ✅ closed in tech-debt batch B, but **not as the row predicted**: only 1 of the 3 allows was inert and dropped (on `pudu()`). The two on `GOOD_CONFIG` and `project` are load-bearing and stay, because every file under `tests/` compiles as its own crate — `tests/init.rs` includes `common` but uses neither, so removing them fails `-D warnings` with `constant GOOD_CONFIG is never used` / `function project is never used` (verified by removal during the 2026-08-30 audit). The comment in `tests/common/mod.rs` records why. |
| TD-S0-08 | 2026-08-30 | ~~closed~~ | A non-sequence `supportedArchitectures` axis (`os: linux` rather than `os: [linux]`) errors with a misleading message and no warning; a non-mapping `supportedArchitectures` block is ignored silently. — ✅ closed in S2 (`DeriveWarning::AxisNotASequence` names the axis and shows the bracket form; `SupportedArchitecturesNotAMapping` replaces the silent ignore) |
| TD-S0-09 | 2026-08-30 | ~~closed~~ | The unknown-**cpu** value warning arm still has no test, and `axis()` silently drops non-string entries (`os: [123]`), also untested. (Narrowed by the 2026-08-30 audit: the `os` and `libc` arms are covered now — `DeriveWarning::UnknownOs` by `no_usable_platforms_exits_three_and_surfaces_the_warnings` in `tests/init.rs`, `UnknownLibc` by `unusable_libc_axis_names_the_axis_and_surfaces_warnings` in `src/cli/init.rs`.) — ✅ closed in S2 (`UnknownCpu` now has a test; `axis()` reports dropped non-string entries via `NonStringAxisEntry` once per axis rather than discarding them) |
| TD-S0-10 | 2026-08-30 | S5 | CRLF `toolchains/BUCK` files gain one stray blank line on the first `--force` (the `\r` is not consumed by the newline check). Self-converges on the second run. |
| TD-S0-11 | 2026-08-30 | ~~closed~~ | `AppendOutcome::ExistingToolchain(String)` always carries the hardcoded `"node"` rather than the user's actual target name, so `pudu init`'s guidance message always prints `:node`. — ✅ closed in the S0 final fix wave (`ExistingToolchain { name, parsed }` carries the parsed target name) |
| TD-S0-12 | 2026-08-30 | S5 | Toolchain detection widened to find-then-check-remainder now false-positives on `not_system_node_toolchain(` (no left-boundary check). Safe direction — it refuses to write — but closable by checking the preceding character. |
| TD-S0-13 | 2026-08-30 | S5 | `AlreadyManaged` reports on marker presence, not block-content equality, so a stale block from an older pudu is reported as present until someone passes `--force`. |
| TD-S0-14 | 2026-08-30 | ~~closed~~ | Spec §3.3 says toolchain detection covers "a `system_node_toolchain(` call **or a target named `node`**"; only the first half is implemented. — ✅ closed in the S0 final fix wave (the unimplemented "target named `node`" clause was struck from spec §3.3) |
| TD-S0-15 | 2026-08-30 | ~~closed~~ | The replace path's span still uses first-occurrence `find` offsets, safe only because the marker-count gate guarantees uniqueness. Add a comment if that gate is ever relaxed. — ✅ closed in the S0 final fix wave (comment added at `src/cli/toolchain.rs`) |
| TD-S0-16 | 2026-08-30 | ~~closed~~ | `config_check` formats parse failures as `{e}: {source_message()}`, but `source_message()` falls back to `to_string()` when there is no `#[source]`, so a sourceless `ConfigError` prints its message twice. One-line guard: only append when the two differ. — ✅ closed in tech-debt batch A (`source_message()` deleted; `error::full_message` joins the cause chain, so a sourceless error renders once) |
| TD-S0-17 | 2026-08-30 | ~~closed~~ | Three error idioms coexist: typed `ConfigError` (thiserror), `anyhow` at the CLI boundary, and `derive_platforms` returning `Result<_, String>`. Warnings are `Vec<String>` throughout, so warning tests must assert on message text. Settle on one shape before S1–S4 each add warnings. — ✅ closed in tech-debt batch A (`DeriveError`/`DeriveWarning`/`ConfigWarning` typed enums in `src/error.rs`; the `String` idiom is gone and warning tests assert on variants) |
| TD-S0-18 | 2026-08-30 | ~~closed~~ | `miette` is derived on `ConfigError` but never rendered — `main` uses `eprintln!("error: {e:#}")`. Every `#[diagnostic(help(...))]` string and `code(...)` value is therefore unreachable. Either wire miette in as spec §6 describes, or drop the derive. — ✅ closed in tech-debt batch A (wired in: `error::render` renders through `GraphicalReportHandler`, used by both `main` and `config check`, so `code`/`help`/`related` reach the user) |
| TD-S0-19 | 2026-08-30 | ~~closed~~ | Every failure exits 2 — usage errors, validation failures, unimplemented stubs, and internal I/O errors are indistinguishable to a CI script. Reserve distinct codes before `--check` semantics land. — ✅ closed in tech-debt batch A (0/1/2/3/4 scheme in `ExitCode`, documented in stage spec §6.1) |
| TD-S0-20 | 2026-08-30 | ~~closed~~ | MSRV is declared (`rust-version = "1.88"`) but never verified: CI builds only on stable. Nothing stops a contributor landing a newer-than-1.88 feature. A `cargo check` job pinned to 1.88 would close it. — ✅ closed in tech-debt batch B (`msrv` job in `.github/workflows/ci.yml` runs `cargo check --all-targets` on `dtolnay/rust-toolchain@1.88`; verified locally the crate compiles clean on 1.88) |
| TD-S0-21 | 2026-08-30 | ~~closed~~ | 15 of 22 declared dependencies (`reqwest`, `gix`, `tar`, `flate2`, `sha2`, `base64`, `glob`, `walkdir`, `nodejs-semver`, `toml_edit`, `dirs`, `httpmock`, …) are unused at S0. A first-time contributor compiles rustls and gix to run a TOML parser. Consider deferring each to the stage that needs it. — ✅ closed in tech-debt batch B: removed `reqwest`, `sha2`, `base64`, `tar`, `flate2` (return for **S3** tarball fetching), `gix`, `dirs` (return for **S8** git registry), `nodejs-semver` (return for **S1**/**S2** version handling), `toml_edit` (returns for **S4**, where TD-S0-05's format-preserving `pudu.toml` round-trip needs it — nothing to do with fixups), `glob`, `walkdir` (return for **S7**, discovering local fixups under `third-party/js/fixups/*/fixups.toml`, design §9; S8a reuses the same walk over a community checkout), and dev-dependency `httpmock` (return for **S3**'s tests) — 12 dependencies total. Dev-dependency `predicates` was missed by that sweep and removed in the final fix wave (zero references in `src/` or `tests/`; the suite passes without it). Verified by removal: `cargo check --all-targets` and the full suite (113 tests) pass after the edit; `insta` and `tempfile` were left in place as still-used. |
| TD-S0-22 | 2026-08-30 | S4 | `init` interpolates a parsed toolchain target name into `pudu.toml` unescaped; pathological Starlark names (`my node`, `bad:name`) round-trip because label validation is looser than Buck's. |
| TD-S0-23 | 2026-08-30 | S4 | `bzl_label` derives from `default_third_party_dir()` rather than a configured value — correct for `init` today, but needs revisiting if `init` ever grows a `--third-party-dir` flag. |
| TD-S0-24 | 2026-08-30 | S4 | Partial scaffolding on I/O failure: if writing `toolchains/BUCK` fails, `pudu.toml` and `third-party/js/` already exist, and re-running reports "pudu.toml already exists; pass --force" — pushing the user toward `--force` to recover from pudu's own partial write. |
| TD-S1-01 | 2026-08-31 | S3 | `parse_lockfile` deserializes the same YAML text three times — once as `Probe` for the version and feature gates, once as `Lockfile`, once as a generic `Value` to find unknown top-level keys. Correct and never on a hot path at S1, but a 8000-line lockfile pays it three times; revisit if lockfile parsing shows up in a profile. |
| TD-S1-02 | 2026-08-31 | S2 | `RawVersion::Num` renders an unquoted YAML version via `format!("{n:.1}")`, so a hypothetical two-decimal `lockfileVersion: 9.10` would be reported as `"9.1"` in the unsupported-version error. Harmless under pnpm's actual versioning, wrong if that ever changes. |
| TD-S1-03 | 2026-08-31 | S4 | `MAX_LEN_WITHOUT_HASH` hardcodes pnpm's `virtual-store-dir-max-length` default of 120. A project that has changed the pnpm setting gets target names that stay internally consistent but no longer match its own `node_modules/.pnpm/`. Cosmetic — greppability is lost, nothing breaks. Expose as config if anyone asks. |
| TD-S1-04 | 2026-08-31 | S5 | `is_link_specifier` treats every `file:` specifier as naming another importer, but `file:./vendor/foo.tgz` is a real package with a `packages:` entry. Its root gets `target: None` and the package becomes unreachable from roots. Harmless at S1 (roots are recorded, never consumed); it will bite when S5 makes workspace links real. Spec §6.3's prose overstates the case and should be corrected then. |
| TD-S1-05 | 2026-08-31 | git-deps | The last-`@` split mis-parses a dep path embedding `git@host` — `mypkg@git+ssh://git@gitlab.com/u/r.git#abcdef` yields name `mypkg@git+ssh://git`. `canonical()` still round-trips so lookup and target naming are correct; only `name`/`version` are wrong. Unverified whether pnpm v9 emits keys of this shape (GitHub normalises to a codeload URL with no `@`). Recheck when S3 meets a real git dependency. |
| TD-S1-06 | 2026-08-31 | S4 | Cycle detection reports one cycle per DFS back edge, not an exhaustive enumeration of simple cycles: a cycle reachable only via a cross edge into a finished node is never found. Documented at `find_cycles` and in spec §7. Fine while `cycles` is a diagnostic; needs Johnson's algorithm if any stage ever depends on completeness. |
| TD-S1-07 | 2026-08-31 | ~~closed~~ | `SnapshotEntry.transitive_peer_dependencies` is parsed but never surfaced on `Node` or `Graph`, reachable only from `Lockfile`. Likewise `engines`, `peer_dependencies`, and `peer_dependencies_meta` are carried on `PackageMeta` but asserted by no test. Decide per field in S2 whether to surface or drop. — ✅ closed in S2 as a decision (spec §7.1): `engines` stays parsed and unused because node version is not a platform axis, and the peer-dependency fields are not surfaced by S2. See TD-S2-02 for the measurable consequence. |
| TD-S1-08 | 2026-08-31 | S3 | `debug::print_graph` emits `"lockfile_version": SUPPORTED_VERSION` — the constant, not an observation from the file — so the field can never disagree with the binary and the test asserting it cannot fail. Harmless while the gate admits only 9.0; revisit if a second version is ever supported. |
| TD-S2-01 | 2026-08-31 | S4 | Pruning has no transitive-reachability sweep: a package that survives its own platform check but whose only parent was pruned stays in the view. Validated as correct against four captured oracles, but all 90 platform-gated keys in the fixture are leaves, so the fixture cannot distinguish this design from one with a sweep. Failure mode if a gated package with a subtree ever appears is an orphan left in the store — fat, not incorrect. Revisit when S4 emits per-package targets. |
| TD-S2-02 | 2026-08-31 | S5 | Pudu does not model `engines`, so its survivor set is a superset of pnpm's by the optional dependencies pnpm skips for node-version reasons. Deliberate (node version is not a platform axis), and the oracle test subtracts them, but it means a package pnpm would never install can reach a generated target. Revisit when S5 makes the node toolchain real. |
| TD-S2-03 | 2026-08-31 | S3 | `libc` pruning is best-effort: npm's abbreviated packument omits the field, so most v9 lockfiles carry no `libc` at all and musl builds are indistinguishable from gnu ones. Pudu matches `libc` when present and never infers it from a package name. A private registry serving full metadata gets stricter pruning than the public one, from the same lockfile shape. |
| TD-S2-04 | 2026-08-31 | S3 | `oracle/engine-excluded.mjs` and `tests/fixtures/platform/reference.mjs` both reach into `@pnpm/package-is-installable`'s internal `lib/*.js` by filesystem path, because the published package's `exports` map blocks deep imports. Documented at both call sites. The internal layout could move in any release of that package, breaking both developer scripts at once (not CI — neither runs there). |
| TD-S2-05 | 2026-08-31 | ~~closed~~ | `tests/fixtures/platform/reference.mjs` has no guard for a host where `detect-libc` reports `unknown` (macOS). pnpm skips the libc axis entirely on such a host, so re-running the fuzz there produces a wall of disagreements that read as "`admits` is broken" rather than "wrong host". A one-line assertion when the detected libc is `unknown` would make the real cause obvious. — ✅ closed in the final fix wave: `reference.mjs` calls `detect-libc`'s `familySync()` (the same call `checkPlatform.js` makes) before reading any cases, and exits 1 with an explanation when it reports `unknown`. Verified on the CI-shape Linux host: `familySync()` returns `glibc`, no behavior change; the guard is exercised by code review since this sandbox has no `unknown`-libc host to hand. |
| TD-S2-06 | 2026-08-31 | S4 | Two configured platforms can emit byte-identical constraint labels: a platform declaring no `libc` never triggers the conditional-abi rule (spec §5.2), so `[platforms.linux-x64]` and `[platforms.linux-x64-musl]` both emit only the linux + x86_64 pair while pruning differently. `config check`'s duplicate detection is keyed on the `(os, cpu, libc)` triple and does not see it. At S4 this becomes a `select()` with two identical arms — a Buck ambiguity error, or a silent first-match win that drops the musl node set. Decide there whether to reject the config or disambiguate the labels. |
| TD-S3-01 | 2026-08-31 | S8+ | `pudu vendor` sends no credentials. `[registry]` supplies hosts only, so a scoped override pointing at a private registry that requires authentication cannot be vendored from; a 401/403 errors with help text saying auth is unimplemented and naming the host. The chosen follow-up is reading `~/.npmrc` — `_authToken` lines, scope→registry mappings, `${VAR}` interpolation, and npm's precedence rules — rather than an ad-hoc `PUDU_TOKEN_*` scheme, so pudu honours the configuration users already have. Decided with the user at S3 spec time. |
| TD-S3-02 | 2026-09-01 | ~~closed~~ | `read_archive` originally rejected any tarball not rooted at literally `package/`. The task 10 live-registry oracle (`tests/vendor_oracle.rs`) found real registry tarballs that violate that: every `@types/*` package, published by DefinitelyTyped's types-publisher, nests under its own display name instead (`@types/estree` → `estree/`, `@types/node` → `node v22.20/`). All four `@types/*` packages in `tests/fixtures/lock/real/pnpm-lock.yaml` hit this and failed the whole vendor pass. — ✅ closed same day: `read_archive` now accepts any archive whose entries share one consistent root directory, whatever its name, and only rejects genuinely mixed-root archives (`src/tarball.rs`). No `strip_prefix` is emitted anywhere yet (build-rule codegen is unwritten), so this only unblocks vendoring; **S4's codegen must record or recompute each package's actual root — it cannot hardcode `"package"` as `strip_prefix`.** |
| TD-S3-03 | 2026-09-01 | S4 | `fsevents@2.3.3`'s registry manifest (`https://registry.npmjs.org/fsevents/2.3.3`) reports `scripts.install: "node-gyp rebuild"` and `gypfile: true`, but its published tarball — sha512-verified against both the lockfile's integrity and the manifest's own `dist.integrity` — contains neither an `install` script nor a `binding.gyp`; it ships a prebuilt `fsevents.node` instead. Verified against pnpm's actual `pkgRequiresBuild` (`@pnpm/building.pkg-requires-build`, called from `pnpm11/worker/src/start.ts` with a manifest and file index both read back from the *extracted tarball*, never the registry API): real pnpm would also compute `false` here. pudu's tarball-derived `has_install_script = false` for this package is correct; the registry's own packument is stale for this one field on this one version. `tests/vendor_oracle.rs` documents and special-cases it rather than treating it as a pudu bug. Revisit if a future npm publish corrects the packument (the special-case would then quietly become inert, not wrong) or if this pattern turns out to recur on other packages S4+ vendors. |
